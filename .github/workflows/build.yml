name: Build iOS dylib

on:
  push:
    branches: [ main ]
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: List files (debug)
        run: ls -la

      - name: Download fishhook
        run: |
          curl -L -o fishhook.h https://raw.githubusercontent.com/facebook/fishhook/main/fishhook.h
          curl -L -o fishhook.c https://raw.githubusercontent.com/facebook/fishhook/main/fishhook.c
          ls -la fishhook.*

      - name: Build dylib
        run: |
          SDK_PATH=$(xcrun --sdk iphoneos --show-sdk-path)
          clang -arch arm64 \
                -isysroot $SDK_PATH \
                -dynamiclib \
                -miphoneos-version-min=12.0 \
                -Os \
                -v \
                -o libunlock.dylib \
                unlock.c fishhook.c 2>&1 | tee build.log
          cat build.log  # in log ra để xem lỗi

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: libunlock
          path: libunlock.dylib
